# B2 Configuration
# Automatically generated by bootstrap.sh

import option ;
import feature ;

# Compiler configuration. This definition will be used unless
# you already have defined some toolsets in your user-config.jam
# file.
if ! gcc in [ feature.values <toolset> ]
{
    using gcc ; 
}

project : default-build <toolset>gcc ;

# List of --with-<library> and --without-<library>
# options. If left empty, all libraries will be built.
# Options specified on the command line completely
# override this variable.
libraries =  --with-program_options --with-timer --with-chrono --with-thread ;

# These settings are equivalent to corresponding command-line
# options.
option.set prefix : /home/guanqin/compareM/Marabou/tools/boost-1.84.0/installed ;
option.set exec-prefix : /home/guanqin/compareM/Marabou/tools/boost-1.84.0/installed ;
option.set libdir : /home/guanqin/compareM/Marabou/tools/boost-1.84.0/installed/lib ;
option.set includedir : /home/guanqin/compareM/Marabou/tools/boost-1.84.0/installed/include ;

# Stop on first error
option.set keep-going : false ;
